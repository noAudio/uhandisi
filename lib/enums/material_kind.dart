/// This enum determines the kind of material that
/// the user wants to be displayed.
///
/// `Raw` displays all `MaterialItem`s that have their
/// `kind` property set to 'Raw'. Same goes for
/// `Encoded` and `Manufactured`.
enum MaterialKind { raw, encoded, manufactured }
