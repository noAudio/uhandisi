import 'package:flutter/material.dart';

class ExperimentalsModal extends StatelessWidget {
  const ExperimentalsModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
